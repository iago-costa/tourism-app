<!-- fluxo-ai-orchestrator: backend-python -->
# backend-python — FastAPI, SQLAlchemy, Alembic, pytest

> Gerado/sincronizado por `tools/agent_orchestrator` — não editar manualmente sem atualizar `registry.json`.

Orquestrador: detecte esta regra quando keywords/paths do registry coincidirem com a solicitação.
Carregue os agentes listados em `.agents/` e respeite `.cursor/rules/backend-python.mdc` quando existir.



## Agentes vinculados (auto)

| Agente | Caminho |
|--------|---------|
| backend-engineer | `.agents/agents/backend/backend-engineer.md` |
| security-engineer | `.agents/agents/security/security-engineer.md` |

## Contexto

Leia `.agents/tourism-app/CONTEXT.md` e `AGENTS.md`.
