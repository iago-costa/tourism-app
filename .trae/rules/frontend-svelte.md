<!-- fluxo-ai-orchestrator: frontend-svelte -->
# frontend-svelte — SvelteKit, TypeScript, Tailwind

> Gerado/sincronizado por `tools/agent_orchestrator` — não editar manualmente sem atualizar `registry.json`.

Orquestrador: detecte esta regra quando keywords/paths do registry coincidirem com a solicitação.
Carregue os agentes listados em `.agents/` e respeite `.cursor/rules/frontend-svelte.mdc` quando existir.



## Agentes vinculados (auto)

| Agente | Caminho |
|--------|---------|
| frontend-engineer | `.agents/agents/frontend/frontend-engineer.md` |
| ui-engineer | `.agents/agents/frontend/ui-engineer.md` |

## Contexto

Leia `.agents/tourism-app/CONTEXT.md` e `AGENTS.md`.
