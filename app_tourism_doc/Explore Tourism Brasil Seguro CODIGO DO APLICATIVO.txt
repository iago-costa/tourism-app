// Explore Tourism Brasil Seguro - Aplicativo Completo

// Explore Tourism Brasil Seguro - Aplicativo Completo

import React from "react";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { MapContainer, TileLayer, Marker, Popup } from "react-leaflet";
import "leaflet/dist/leaflet.css";

// Mock de dados de lugares turísticos
const locations = [
  {
    id: 1,
    name: "Cristo Redentor",
    lat: -22.9519,
    lng: -43.2105,
    description: "Um dos pontos turísticos mais famosos do Brasil.",
    image: "/images/cristo.jpg",
  },
  {
    id: 2,
    name: "Parque Ibirapuera",
    lat: -23.5874,
    lng: -46.6576,
    description: "Grande parque urbano em São Paulo.",
    image: "/images/ibirapuera.jpg",
  },
];

function HomePage() {
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold mb-4">Explore Tourism Brasil Seguro</h1>
      <p className="mb-4">
        Descubra os melhores pontos turísticos do Brasil com segurança e
        praticidade.
      </p>
      <MapContainer
        center={[-15.7797, -47.9297]}
        zoom={4}
        className="h-96 w-full rounded-xl"
      >
        <TileLayer url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />
        {locations.map((loc) => (
          <Marker key={loc.id} position={[loc.lat, loc.lng]}>
            <Popup>
              <strong>{loc.name}</strong>
              <p>{loc.description}</p>
              <img
                src={loc.image}
                alt={loc.name}
                className="w-full mt-2 rounded"
              />
            </Popup>
          </Marker>
        ))}
      </MapContainer>
      <div className="mt-6 grid grid-cols-1 sm:grid-cols-2 gap-4">
        {locations.map((loc) => (
          <Card key={loc.id}>
            <CardContent>
              <img
                src={loc.image}
                alt={loc.name}
                className="rounded-xl mb-2"
              />
              <h2 className="font-semibold text-lg">{loc.name}</h2>
              <p className="text-sm text-gray-600">{loc.description}</p>
              <Button className="mt-2 w-full">Explorar</Button>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  );
}

export default function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<HomePage />} />
      </Routes>
    </Router>
  );
}
